<%= form_with url: admin_users_path, method: :get do |form| %>
  <div class="max-w-sm">
    <%= form.label :query, t(".help"), class: "block text-sm font-medium text-blue-950" %>
    <div class="mt-1 flex rounded-md shadow-sm">
      <div class="relative flex items-stretch flex-grow focus-within:z-10">
        <%= form.text_field :query, value: query, placeholder: "Joe Masilotti", autocomplete: "off", autofocus: true, class: "focus:ring-blue-500 focus:border-gray-500 block w-full rounded-none rounded-l-md sm:text-sm border-gray-300" %>
      </div>
      <%= form.button type: "submit", class: "-ml-px relative inline-flex items-center space-x-2 px-4 py-2 border border-gray-300 text-sm font-medium rounded-r-md text-blue-950 bg-blue-50 hover:bg-blue-100 focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-gray-500" do %>
        <%= inline_svg_tag "icons/solid/search.svg", class: "h-5 w-5 text-blue-400", aria_hidden: true %>
        <span><%= t(".cta") %></span>
      <% end %>
    </div>
  </div>
<% end %>
